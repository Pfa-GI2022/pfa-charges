import { SearchFilterModulesPipe } from './search-filter-modules.pipe';

describe('SearchFilterModulesPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchFilterModulesPipe();
    expect(pipe).toBeTruthy();
  });
});
