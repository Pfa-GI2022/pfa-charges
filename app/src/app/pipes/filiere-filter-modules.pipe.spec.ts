import { FiliereFilterModulesPipe } from './filiere-filter-modules.pipe';

describe('FiliereFilterModulesPipe', () => {
  it('create an instance', () => {
    const pipe = new FiliereFilterModulesPipe();
    expect(pipe).toBeTruthy();
  });
});
